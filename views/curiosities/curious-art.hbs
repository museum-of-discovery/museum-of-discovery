<h1>Curious Art</h1>

{{#each curiosities}}
<section>
    <h2>{{this.title}}</h2>
    <img src="{{this.image}}" alt="{{this.title}}" />
    <span>
        {{this.date}}
    </span>
    <p>{{this.description}}</p>

</section>
{{/each}}